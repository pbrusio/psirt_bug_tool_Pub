# Scanner Architecture - Visual Diagrams

## 1. Dual-Path Request Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                         API REQUEST                                 │
│  POST /api/v1/scan-device     OR     POST /api/v1/analyze-psirt    │
└────────────────────┬────────────────────────────────────────────────┘
                     │
                     ▼
        ┌────────────────────────┐
        │ VulnerabilityScanner   │
        │  (Route Decision)      │
        └────────────┬───────────┘
                     │
        ┌────────────┴────────────┐
        │                         │
        ▼                         ▼
┌──────────────┐          ┌──────────────┐
│   PATH A:    │          │   PATH B:    │
│ SCAN DEVICE  │          │ ANALYZE      │
│  (Fast)      │          │  PSIRT       │
└──────┬───────┘          │  (Slow)      │
       │                  └──────┬───────┘
       │                         │
       │                         ▼
       │                  ┌──────────────┐
       │                  │ Check DB     │
       │                  │ Cache?       │
       │                  └──────┬───────┘
       │                         │
       │                  ┌──────┴──────┐
       │                  │             │
       │            Cache Hit      Cache Miss
       │                  │             │
       ▼                  ▼             ▼
┌─────────────┐    ┌─────────────┐  ┌─────────────┐
│ SQLite DB   │    │ Return      │  │ SEC-8B      │
│ Query:      │    │ Cached      │  │ Inference   │
│             │    │ Result      │  │ (~3.4s)     │
│ 1. Version  │    │ (<10ms)     │  └──────┬──────┘
│    Index    │    └─────────────┘         │
│ 2. Label    │                            │
│    Index    │                     ┌──────▼──────┐
│ 3. Vulns    │                     │ High        │
│    Join     │                     │ Confidence? │
│             │                     │ (≥0.75)     │
│ (<100ms)    │                     └──────┬──────┘
└──────┬──────┘                            │
       │                              ┌────┴────┐
       │                             Yes       No
       │                              │         │
       │                        ┌─────▼────┐   │
       │                        │ Cache to │   │
       │                        │ Database │   │
       │                        └─────┬────┘   │
       │                              │        │
       └──────────────┬───────────────┴────────┘
                      │
                      ▼
             ┌─────────────────┐
             │ Group by        │
             │ Severity        │
             │ - Critical/High │
             │ - Medium/Low    │
             └────────┬────────┘
                      │
                      ▼
             ┌─────────────────┐
             │ Return Results  │
             │ - Full details  │
             │ - Collapsed     │
             │ - Pagination    │
             └─────────────────┘
```

## 2. Database Query Strategy (Path A)

```
┌─────────────────────────────────────────────────────────────┐
│ INPUT: platform="IOS-XE", version="17.3.5",                 │
│        labels=["MGMT_SSH_HTTP", "SEC_CoPP"]                 │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
                 ┌──────────────┐
                 │ Parse Version│
                 │ "17.3.5" →   │
                 │ (17, 3, 5)   │
                 └──────┬───────┘
                        │
                        ▼
         ┌──────────────────────────┐
         │ STEP 1: Version Match    │
         │ ┌──────────────────────┐ │
         │ │ SELECT DISTINCT      │ │
         │ │   vuln_id            │ │
         │ │ FROM version_index   │ │
         │ │ WHERE platform='IOS-XE'│
         │ │   AND major=17       │ │
         │ │   AND minor=3        │ │
         │ │   AND (patch IS NULL │ │
         │ │        OR patch<=5)  │ │
         │ └──────────────────────┘ │
         └───────────┬──────────────┘
                     │
                     ▼
           ┌─────────────────┐
           │ Result:         │
           │ [vuln-001,      │
           │  vuln-015,      │
           │  vuln-042,      │
           │  ... 50 total]  │
           └────────┬────────┘
                    │
                    ▼
         ┌──────────────────────────┐
         │ STEP 2: Label Filter     │
         │ ┌──────────────────────┐ │
         │ │ SELECT vuln_id       │ │
         │ │ FROM label_index     │ │
         │ │ WHERE vuln_id IN (   │ │
         │ │   vuln-001, ...      │ │
         │ │ )                    │ │
         │ │ AND label IN (       │ │
         │ │   'MGMT_SSH_HTTP',   │ │
         │ │   'SEC_CoPP'         │ │
         │ │ )                    │ │
         │ └──────────────────────┘ │
         └───────────┬──────────────┘
                     │
                     ▼
           ┌─────────────────┐
           │ Result:         │
           │ [vuln-001,      │
           │  vuln-015,      │
           │  ... 12 total]  │
           └────────┬────────┘
                    │
                    ▼
         ┌──────────────────────────┐
         │ STEP 3: Join & Enrich    │
         │ ┌──────────────────────┐ │
         │ │ SELECT v.*, li.label │ │
         │ │ FROM vulnerabilities v│ │
         │ │ LEFT JOIN label_index │ │
         │ │   ON v.vuln_id =     │ │
         │ │      li.vuln_id      │ │
         │ │ WHERE v.vuln_id IN ( │ │
         │ │   vuln-001, ...      │ │
         │ │ )                    │ │
         │ │ GROUP BY v.vuln_id   │ │
         │ └──────────────────────┘ │
         └───────────┬──────────────┘
                     │
                     ▼
        ┌────────────────────────┐
        │ Full Records:          │
        │ ┌────────────────────┐ │
        │ │ vuln-001           │ │
        │ │ - advisory_id      │ │
        │ │ - severity: 1      │ │
        │ │ - cvss: 9.8        │ │
        │ │ - labels: [SSH]    │ │
        │ │ - config_regex     │ │
        │ │ - show_commands    │ │
        │ └────────────────────┘ │
        │ ... (12 total)         │
        └────────┬───────────────┘
                 │
                 ▼
       ┌──────────────────┐
       │ Group by Severity│
       └─────────┬────────┘
                 │
         ┌───────┴────────┐
         │                │
         ▼                ▼
┌────────────────┐ ┌────────────────┐
│ Critical/High  │ │ Medium/Low     │
│ (Severity 1-2) │ │ (Severity 3-6) │
│ ────────────── │ │ ────────────── │
│ vuln-001 (S1)  │ │ vuln-042 (S4)  │
│ vuln-015 (S2)  │ │ vuln-089 (S5)  │
│ ... 5 vulns    │ │ ... 7 vulns    │
│                │ │                │
│ FULL DETAILS   │ │ COLLAPSED      │
└────────────────┘ └────────────────┘
```

## 3. Result Grouping and Formatting

```
┌───────────────────────────────────────────────────────────────┐
│                    RAW QUERY RESULTS (12 vulns)               │
│  vuln-001 (S1, 9.8), vuln-015 (S2, 7.5), vuln-042 (S4, 5.3) │
│  vuln-089 (S5, 3.2), ...                                      │
└─────────────────────────┬─────────────────────────────────────┘
                          │
                          ▼
              ┌───────────────────────┐
              │ Sort by:              │
              │ 1. Severity (asc)     │
              │ 2. CVSS Score (desc)  │
              └───────────┬───────────┘
                          │
                  ┌───────┴────────┐
                  │                │
                  ▼                ▼
     ┌────────────────────┐  ┌────────────────────┐
     │  CRITICAL/HIGH     │  │  MEDIUM/LOW        │
     │  (Severity 1-2)    │  │  (Severity 3-6)    │
     └─────────┬──────────┘  └─────────┬──────────┘
               │                       │
               ▼                       ▼
    ┌──────────────────┐    ┌──────────────────┐
    │ Format: FULL     │    │ Format: COLLAPSED│
    └─────────┬────────┘    └─────────┬────────┘
              │                       │
              ▼                       ▼
┌─────────────────────────┐ ┌─────────────────────┐
│ {                       │ │ {                   │
│   vuln_id: "vuln-001"   │ │   vuln_id: "..."    │
│   advisory_id: "..."    │ │   advisory_id: "..."│
│   severity: 1           │ │   severity: 4       │
│   cvss_score: 9.8       │ │   cvss_score: 5.3   │
│   cves: [...]           │ │   summary: "..."    │
│   summary: "..."        │ │ }                   │
│   matched_labels: [     │ │                     │
│     "MGMT_SSH_HTTP"     │ │ ~200 bytes          │
│   ]                     │ │                     │
│   product_names: [...]  │ │ Click to expand:    │
│   fixed_versions: [...]  │ │ GET /vulnerability/ │
│   config_regex: [       │ │      {vuln_id}      │
│     "^ip ssh"           │ │                     │
│   ]                     │ │ Returns full details│
│   show_commands: [      │ │                     │
│     "show ip ssh"       │ │                     │
│   ]                     │ │                     │
│   unlabeled: false      │ │                     │
│   confidence: 0.87      │ │                     │
│ }                       │ │                     │
│                         │ │                     │
│ ~400 bytes              │ │                     │
└─────────────────────────┘ └─────────────────────┘
```

## 4. Cache Decision Flow (Path B)

```
┌────────────────────────────────────────────┐
│ LLM Analysis Complete                      │
│ - advisory_id: "cisco-sa-xyz"              │
│ - confidence: 0.87                         │
│ - predicted_labels: ["MGMT_SSH_HTTP"]      │
└──────────────────┬─────────────────────────┘
                   │
                   ▼
        ┌──────────────────┐
        │ Should Cache?    │
        │ Check 3 criteria │
        └─────────┬────────┘
                  │
      ┌───────────┼───────────┐
      │           │           │
      ▼           ▼           ▼
┌──────────┐ ┌──────────┐ ┌──────────┐
│advisory_id│ │confidence│ │ labels   │
│ present? │ │  ≥ 0.75? │ │ present? │
└─────┬────┘ └─────┬────┘ └─────┬────┘
      │            │            │
   ┌──┴──┐      ┌──┴──┐      ┌──┴──┐
   │ YES │      │ YES │      │ YES │
   └──┬──┘      └──┬──┘      └──┬──┘
      │            │            │
      └────────────┼────────────┘
                   │
                   ▼
           ┌───────────────┐
           │ ALL 3 = YES?  │
           └───────┬───────┘
                   │
         ┌─────────┴─────────┐
         │                   │
        YES                 NO
         │                   │
         ▼                   ▼
  ┌──────────────┐    ┌──────────────┐
  │ CACHE IT     │    │ DON'T CACHE  │
  └──────┬───────┘    └──────┬───────┘
         │                   │
         ▼                   ▼
  ┌──────────────┐    ┌──────────────┐
  │ INSERT INTO: │    │ Return result│
  │              │    │ with:        │
  │ vulnerabili- │    │ cached=false │
  │ ties table   │    └──────────────┘
  │              │
  │ label_index  │
  │              │
  │ version_index│
  └──────┬───────┘
         │
         ▼
  ┌──────────────┐
  │ Return result│
  │ with:        │
  │ cached=true  │
  │ source="llm" │
  └──────────────┘

EXAMPLES:
─────────────────────────────────────────────
✅ CACHE: advisory_id="cisco-sa-xyz",
         confidence=0.87, labels=["MGMT_SSH"]

❌ NO CACHE: advisory_id=null (ad-hoc query)
            confidence=0.92, labels=[...]

❌ NO CACHE: advisory_id="cisco-sa-abc"
            confidence=0.62 (MEDIUM)
            labels=[...]

❌ NO CACHE: advisory_id="cisco-sa-def"
            confidence=0.88
            labels=[] (none predicted)
```

## 5. Frontend UI Layout

```
┌─────────────────────────────────────────────────────────────┐
│                    Device Vulnerability Scan                 │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Platform: [IOS-XE ▼]  Version: [17.3.5]  [Scan Device]     │
│                                                              │
│  Configured Features (auto-detected from snapshot):          │
│  ☑ MGMT_SSH_HTTP  ☑ SEC_CoPP  ☑ RTE_OSPF  ☑ L2_STP         │
│  ☑ IP_DHCP_Server  ☐ APP_IOx  ☐ SEC_VPN_IKEv2              │
│                                                              │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ⚠️  12 Vulnerabilities Found (5 Critical/High, 7 Medium/Low)│
│      Query time: 87ms                                        │
│                                                              │
├─────────────────────────────────────────────────────────────┤
│  CRITICAL & HIGH (5) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌────────────────────────────────────────────────────┐     │
│  │ 🔴 cisco-sa-iosxe-ssh-dos (Severity 1, CVSS 9.8)   │     │
│  │                                                     │     │
│  │ CVE-2023-12345 | Published: 2023-09-15             │     │
│  │                                                     │     │
│  │ Summary: Denial of Service vulnerability in SSH... │     │
│  │                                                     │     │
│  │ Matched Features: MGMT_SSH_HTTP                    │     │
│  │ Fixed in: 17.3.6                                   │     │
│  │                                                     │     │
│  │ ▼ Config Checks (2)                                │     │
│  │   • ^ip ssh                                        │     │
│  │   • ^line vty                                      │     │
│  │                                                     │     │
│  │ ▼ Show Commands (2)                                │     │
│  │   • show ip ssh                                    │     │
│  │   • show line vty                                  │     │
│  │                                                     │     │
│  │ [Verify on Device] [View Advisory] [Dismiss]       │     │
│  └────────────────────────────────────────────────────┘     │
│                                                              │
│  ┌────────────────────────────────────────────────────┐     │
│  │ 🟠 cisco-sa-iosxe-ospf-dos (Severity 2, CVSS 7.5)  │     │
│  │ ...                                                 │     │
│  └────────────────────────────────────────────────────┘     │
│                                                              │
│  ... (3 more Critical/High)                                  │
│                                                              │
├─────────────────────────────────────────────────────────────┤
│  MEDIUM & LOW (7) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│  [Expand to view] ▶                                          │
├─────────────────────────────────────────────────────────────┤
│  (Collapsed by default - click to expand)                    │
│                                                              │
│  When expanded:                                              │
│  • CSCabc12345 (Sev 4) - Information disclosure in OSPF     │
│  • CSCdef67890 (Sev 5) - Minor logging issue                │
│  ... (5 more)                                                │
│                                                              │
│  [Click any to view full details]                            │
└─────────────────────────────────────────────────────────────┘
```

## 6. Component Integration Map

```
┌─────────────────────────────────────────────────────────────┐
│                     FRONTEND (React)                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ Scan Form    │  │ Results View │  │ Verify Modal │      │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘      │
└─────────┼──────────────────┼──────────────────┼─────────────┘
          │                  │                  │
          │ POST /scan       │ GET /vuln/{id}   │ POST /verify
          │                  │                  │
┌─────────▼──────────────────▼──────────────────▼─────────────┐
│                   FASTAPI ROUTES                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │/scan-device  │  │/vulnerability│  │/verify-device│      │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘      │
└─────────┼──────────────────┼──────────────────┼─────────────┘
          │                  │                  │
          │                  │                  │
┌─────────▼──────────────────▼──────────────────▼─────────────┐
│            VULNERABILITY SCANNER (NEW)                       │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │scan_device() │  │get_vuln_     │  │(uses existing│      │
│  │              │  │details()     │  │ verifier)    │      │
│  └──────┬───────┘  └──────┬───────┘  └──────────────┘      │
└─────────┼──────────────────┼──────────────────┬─────────────┘
          │                  │                  │
    ┌─────┴─────┐      ┌─────┴─────┐      ┌─────▼─────┐
    │           │      │           │      │           │
    ▼           ▼      ▼           ▼      ▼           ▼
┌─────────┐ ┌─────────┐  ┌─────────┐ ┌─────────┐ ┌─────────┐
│Database │ │ SEC-8B  │  │Database │ │ SEC-8B  │ │ Device  │
│ Query   │ │Analyzer │  │ Lookup  │ │Analyzer │ │Verifier │
│ (Fast)  │ │ (Slow)  │  │         │ │(Cached) │ │ (SSH)   │
└─────┬───┘ └────┬────┘  └────┬────┘ └────┬────┘ └────┬────┘
      │          │            │           │           │
      ▼          ▼            ▼           ▼           ▼
┌──────────────────────────────────────────────────────────┐
│                  SQLITE DATABASE                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │
│  │vulnerabilities│  │version_index │  │label_index   │   │
│  │(2,654 rows)  │  │(indexed)     │  │(indexed)     │   │
│  └──────────────┘  └──────────────┘  └──────────────┘   │
└──────────────────────────────────────────────────────────┘

EXISTING COMPONENTS (No changes required):
─────────────────────────────────────────────
• SEC8BAnalyzer (backend/core/sec8b.py)
• DeviceVerifier (backend/core/verifier.py)
• In-memory cache (backend/db/cache.py)

NEW COMPONENTS (Phase 2):
─────────────────────────────────────────────
• VulnerabilityScanner (backend/core/vulnerability_scanner.py)
• VulnerabilityDatabase (backend/db/vulnerability_db.py)
• /scan-device endpoint (backend/api/routes.py)
• /vulnerability/{id} endpoint (backend/api/routes.py)

MODIFIED COMPONENTS (Phase 2):
─────────────────────────────────────────────
• /analyze-psirt endpoint (add cache check)
• API models (add ScanDeviceRequest, ScanResult)
```

## 7. Performance Comparison

```
SCENARIO: Network with 100 devices to scan
─────────────────────────────────────────────

WITHOUT DATABASE (Current):
┌──────────────────────────────────────────┐
│ Device 1 → SEC-8B (3.4s) → Result        │
│ Device 2 → SEC-8B (3.4s) → Result        │
│ ...                                      │
│ Device 100 → SEC-8B (3.4s) → Result      │
│                                          │
│ TOTAL TIME: 340 seconds (5.7 minutes)    │
└──────────────────────────────────────────┘

WITH DATABASE (New):
┌──────────────────────────────────────────┐
│ Device 1 → DB Query (87ms) → Result      │
│ Device 2 → DB Query (87ms) → Result      │
│ ...                                      │
│ Device 100 → DB Query (87ms) → Result    │
│                                          │
│ TOTAL TIME: 8.7 seconds                  │
│                                          │
│ SPEEDUP: 39x faster                      │
└──────────────────────────────────────────┘

WITH DATABASE + PARALLELIZATION:
┌──────────────────────────────────────────┐
│ Parallel queries (10 at a time)          │
│                                          │
│ Batch 1 (10 devices) → 100ms             │
│ Batch 2 (10 devices) → 100ms             │
│ ...                                      │
│ Batch 10 (10 devices) → 100ms            │
│                                          │
│ TOTAL TIME: 1 second                     │
│                                          │
│ SPEEDUP: 340x faster                     │
└──────────────────────────────────────────┘
```

## Notes

These diagrams illustrate:

1. **Request routing** - How scanner decides between DB and LLM paths
2. **Query strategy** - Step-by-step database query execution
3. **Result formatting** - How full vs collapsed results differ
4. **Cache logic** - Decision tree for when to cache LLM results
5. **UI layout** - How frontend displays grouped results
6. **System integration** - How new scanner fits with existing components
7. **Performance gains** - Dramatic speedup for batch scanning

All diagrams use ASCII art for maximum compatibility and can be viewed in any text editor.
