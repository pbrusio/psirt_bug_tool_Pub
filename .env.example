# Environment Variables Template for PSIRT Vulnerability Analysis System
# Copy this file to .env and fill in your values
# NEVER commit .env to version control

# =============================================================================
# ISE API Credentials (for device inventory sync)
# =============================================================================
ISE_HOST=your-ise-server.example.com
ISE_USERNAME=ersAdmin
ISE_PASSWORD=

# =============================================================================
# Device SSH Credentials (for verification)
# =============================================================================
DEVICE_HOST=192.168.0.1
DEVICE_USERNAME=admin
DEVICE_PASSWORD=
DEVICE_TYPE=cisco_ios

# =============================================================================
# Optional API Security
# =============================================================================
# Admin API key for protected endpoints (optional)
ADMIN_API_KEY=

# Allowed CORS origins (comma-separated, optional)
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000

# Development mode (set to 'true' to enable debug features)
# DEV_MODE=false
